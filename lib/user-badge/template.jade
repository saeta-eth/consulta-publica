.user-wrapper
  .notifications-badge.pull-left.hidden-xs
    a.btn.notifications(href='#{config.subPath}/notifications')
      span.fa.fa-bell-o.bell-o
  .user-badge.pull-right
    a.btn.profile
      img(src=user.profilePicture())
      - var color = config.headerFontColor
      span.name.ellipsis(style=color ? 'color: #{color}' : '')=user.firstName
      span.caret
    ul.dropdown-list
      li.visible-xs
        a(href='#{config.subPath}/notifications')=t('notifications.title')
      - if (user.staff)
        li: a(href='#{config.subPath}/admin')= t('header.admin')
      li: a(href='#{config.subPath}/ajustes')= t('header.settings')
      - if (config.frequentlyAskedQuestions)
        li: a(href="#{config.subPath}/ayuda/como-funciona")= t('help.faq.title')
      - if (config.aboutUs)
        li: a(href="#{config.subPath}/ayuda/acerca")= t('help.about-us.title')
      - if (config.termsOfService)
        li: a(href="#{config.subPath}/ayuda/terminos-y-condiciones")= t('help.tos.title')
      - if (config.privacyPolicy)
        li: a(href="#{config.subPath}/ayuda/privacidad")= t('help.pp.title')
      - if (config.glossary)
        li: a(href="#{config.subPath}/ayuda/glossary")= t('help.glossary.title')
      li: a(href='#{config.subPath}/logout')= t('header.logout')
